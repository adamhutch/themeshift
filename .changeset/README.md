# Changesets

Use `pnpm changeset` whenever a change should trigger a package release.

- `@themeshift/ui` and `@themeshift/ui-app` are versioned together.
- `@themeshift/vite-plugin-themeshift` is versioned independently.
- Private apps are versioned for coordination but are not published to npm.
